# Client tests

Add UI/component tests here with vitest + @testing-library/react. Pages depend on `QueryClientProvider`, `AuthProvider`, and `TooltipProvider`, so wrap your renders with the same providers as `client/src/App.tsx`.
